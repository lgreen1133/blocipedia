<% if @collaborator.destroyed? %>
  $('#collaborator-' +<%= @collaborator.id %>).hide();
<% else %>
  $('#collaborator-' +<%=@collaborator.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] %>");
<% end %>  
